# Additional requirements for Streamlit web interface
streamlit>=1.28.0
streamlit-ace>=0.1.1
plotly>=5.17.0
watchdog>=3.0.0

# Note: Main requirements are already in requirements.txt
# Run: pip install -r requirements.txt && pip install -r streamlit_requirements.txt